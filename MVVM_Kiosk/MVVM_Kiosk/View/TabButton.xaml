<UserControl x:Class="MVVM_Kiosk.View.TabButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ViewModels="clr-namespace:MVVM_Kiosk.ViewModels" 
             mc:Ignorable="d" 
             Loaded="Tab_Button_Loaded"
             >
    <UserControl.Resources>
        <ResourceDictionary>
            <Style x:Key="Tab_btn" TargetType="ToggleButton">
                <Setter Property="Width" Value="Auto"/>
                <Setter Property="Height" Value="51"/>
                <Setter Property="Content" Value="ING"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <ToggleButton CommandParameter="{Binding}" Style="{StaticResource Tab_btn}"/>
</UserControl>
